# `_attic/` — configs arquivados

Dotfiles que saíram do uso ativo mas ficam aqui pra auditoria/resgate antes do
delete definitivo. Subdir name carrega a data de arquivamento.

| Pasta                          | Motivo                                                    |
|--------------------------------|-----------------------------------------------------------|
| `hyprpanel.2026-05-16/`        | Substituído por waybar (decisão refactor 2026-05-16). Config solto, sem pacote Nix, redundante com a barra principal. |

Quando tiver certeza que não precisa mais: `rm -rf <subdir>`.
