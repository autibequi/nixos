# Ghostty — Lightweight & Performant
font-family = "FiraCode"
font-size = 12
line-height = 1.2
padding = 8

# Cursor
cursor-style = block
cursor-thickness = 2

# Performance
scrollback-limit = 5000
copy-on-select = false

# Theme — clean dark
background = 1e1e1e
foreground = e0e0e0
