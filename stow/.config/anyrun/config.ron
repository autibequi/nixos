Config(
    x: Fraction(0.5), // Centralizado horizontalmente
    y: Fraction(0.5), // Centralizado verticalmente
    width: Absolute(700), // Ligeiramente mais compacto para efeito de "caixa flutuante"
    height: Absolute(400), // Altura reduzida para reforçar o visual flutuante
    hide_icons: false,
    ignore_exclusive_zones: true, // Sobrepõe tudo, sempre visível
    layer: Top, // Prioridade máxima de exibição
    hide_plugin_info: false,
    close_on_click: true,
    show_results_immediately: true,
    max_entries: Some(10), // Ajuste para visual mais limpo
    plugins: [
        "libapplications.so",
        "libshell.so",
        "libwebsearch.so",
        "librink.so",
    ],
    border_radius: Some(22), // Bordas ainda mais suaves para efeito flutuante
    shadow: Some(true), // Sombra acentuada para destacar a caixa
    opacity: Some(0.94), // Mais transparência para reforçar o efeito flutuante
)