# Chromium flags — lido pelo wrapper Nix antes de iniciar o binário
# 1 flag por linha, comentários com #

# Wayland nativo — ver chrome-flags.conf para contexto
--ozone-platform=wayland

# Mesma compensação de scale que o Chrome precisou — ver chrome-flags.conf
--force-device-scale-factor=2
