

# =============================================
#            CONFIGURATION IMPORTS
# =============================================
# Default Monitor
monitor = ,highres,auto,2
source = ~/.config/hypr/monitors.conf

# Core
source = ~/.config/hypr/windowrules.conf
source = ~/.config/hypr/application.conf
source = ~/.config/hypr/systemtools.conf
source = ~/.config/hypr/workspace.conf
source = ~/.config/hypr/special-workspaces.conf

# Plugins
# source = ~/.config/hypr/plugins/hyprexpo.conf
source = ~/.config/hypr/plugins/dynamic-cursors.conf
# source = ~/.config/hypr/plugins/hyprspace.conf
# source = ~/.config/hypr/plugins/hyprtasking.conf
# source = ~/.config/hypr/plugins/hyprtrails.conf

# GPU Selection
# env = AQ_DRM_DEVICES,/dev/dri/card0:/dev/dri/card1

# ===========================================
#            Startup
# ===========================================
# Cursor
exec-once = hyprctl setcursor rose-pine-hyprcursor 50
env = HYPRCURSOR_THEME,rose-pine-hyprcursor
env = HYPRCURSOR_SIZE,50

# session core
exec-once = uwsm app -- waybar
exec-once = uwsm app -- swww-daemon # wallpaper
exec-once = uwsm app -- swaync # notifications
exec-once = uwsm app -- hypridle

# init utils
exec-once = uwsm app -- wl-paste --watch cliphist store # clipboard
exec-once = uwsm app -- hyprshade auto # shaders

# scripts
exec-once = $hyprutils dark_theme

# Services
# exec-once = uwsm app -- lm-studio # for tabcompletion

# ===================================================================
#                  GENERAL WORKSPACE CONFIGURATION
# ===================================================================
general {
    gaps_in                  = 5
    gaps_out                 = 5
    border_size              = 5
    col.active_border        = rgba(7c3aedff) rgba(00ff41ff) 45deg
    # col.inactive_border      = rgba(2d1b4eaa) rgba(003d11aa) 45deg
    layout                   = scrolling #dwindle
    allow_tearing            = true
    resize_on_border         = true
    extend_border_grab_area  = 10
}

scrolling {
    column_width             = 0.5
    fullscreen_on_one_column = true
    focus_fit_method         = 1        # 0 = center, 1 = fit
    follow_focus             = false
    follow_min_visible       = 0.4
    explicit_column_widths   = 0.333, 0.5, 0.667, 1.0
}

# unscale XWayland
# xwayland {
#   force_zero_scaling = true
# }

misc {
    vrr = on
    initial_workspace_tracking = on;
    focus_on_activate = true
}

render {
    new_render_scheduling = true
    direct_scanout = true
}

decoration {
    blur:enabled = false
    shadow:enabled = false
    rounding = 3
}

# ============================================
#                INPUT SETTINGS
# ============================================
# ref: https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout    = us
    kb_variant   = altgr-intl
    kb_options   = caps:hyper # your caps is now MOD3, use wisely
    follow_mouse = 1          # focus only on click, not hover

    scroll_factor = 0.7

    touchpad {
        natural_scroll         = true
        disable_while_typing   = true
        tap-to-click           = true
        drag_lock              = false
        clickfinger_behavior   = false
    }
}

# ===========================================
#           ANIMATION SETTINGS
# ===========================================
animations {
    enabled = true

    # Snappy — fast start, gentle decel (good for 60Hz)
    bezier = snappy, 0.25, 1.0, 0.5, 1.0

    # Smooth — slower ease-out for window open/close
    bezier = smooth, 0.2, 0.9, 0.3, 1.0

    # Overshot — slight bounce at the end
    bezier = overshot, 0.25, 1.0, 0.5, 1.05

    # Linear (Constant speed)
    bezier = linear, 0.0, 0.0, 1.0, 1.0

    animation = windows,      1, 2, smooth,
    animation = windowsOut,   1, 2, smooth, popin 80%
    animation = border,       1, 3, smooth
    animation = borderangle,  0, 3, smooth
    animation = fade,         1, 3, smooth
    animation = workspaces,   1, 3, smooth

    # Magic workspaces
    animation = specialWorkspace, 1, 3, overshot, slidevert
}

# ============================================
#              GESTURE BINDINGS
# ============================================
# Workspace switching
gesture = 3, horizontal, workspace

# Resize
gesture = 4, horizontal, scale:0.5, resize
gesture = 4, vertical, scale:0.5, resize

# Cursor zoom
# gesture = 2, pinchin, cursorZoom
# gesture = 2, pinchout, cursorZoom
