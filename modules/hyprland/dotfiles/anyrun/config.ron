Config(
    x: Fraction(0.5),
    y: Fraction(0.5),
    width: Absolute(700),
    height: Absolute(420),
    hide_icons: false,
    ignore_exclusive_zones: false,
    layer: Top,
    hide_plugin_info: false,
    close_on_click: true,
    show_results_immediately: true,
    max_entries: Some(10),
    plugins: [
        "libapplications.so",
        "libshell.so",
        "libwebsearch.so",
        "librink.so",
    ],
)