# Cursor (só runtime)

- **`cursor-hook.sh`** — adaptador JSON do Cursor → scripts na pasta pai (`../session-start.sh`, etc.).
- **`hooks.json`** — manifest; no container montado em `~/.cursor/hooks.json`.

O resto dos hooks vennon (partilhados com Claude) está em **`../`** (`self/hooks/`).
