---
name: meta:webview
description: Acessar uma URL e ver o conteúdo renderizado — Chrome relay (visual) ou WebFetch (texto). Inspeção DOM via CDP.
---

Ler `self/skills/meta/webview.md` e seguir as instruções.

Se o usuário passou uma URL: fazer live check do relay, abrir com `nav` se online, usar `WebFetch` se offline.
Se nenhuma URL foi passada: mostrar o guia de uso das 3 abordagens (relay, WebFetch, curl).
