---
description:
globs:
alwaysApply: true
---
Always tests changes with `nh os test .` if you need to check if your change is valid.

It's fast and free, so try to use it ever small change made.

You can add new files to nixos recognize it with git add.

never commit, just stage it if necessary