# Diff — Árvore de Arquivos no Chrome

Roda difftree.py e renderiza o output no Chrome com HTML preservando cores ANSI. Extensão visual do estrategia:glance.

## Entrada
- `$ARGUMENTS`: `[--repos monolito|bo|front|all] [--compare origin/main]`

## Instruções

Ler e executar o skill `code/analysis/diff`:

```
Skill: code/analysis/diff
```

Argumentos passados: `$ARGUMENTS`
