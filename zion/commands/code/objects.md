# Objects — Objetos Modificados por Camada

Lista todos os objetos tocados na branch atual, categorizados por repo e camada (handlers, services, repos, workers, pages, components, etc.).

## Entrada
- `$ARGUMENTS`: `[--repo monolito|bo|front|all] [--format terminal|chrome]`

## Instruções

Ler e executar o skill `code/analysis/objects`:

```
Skill: code/analysis/objects
```

Argumentos passados: `$ARGUMENTS`
