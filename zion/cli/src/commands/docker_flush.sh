echo "# This file is located at 'src/commands/docker_flush.sh'."
echo "# It contains the implementation for the 'zion docker flush' command."
echo "# The code you write here will be wrapped by a function named 'zion_docker_flush_command()'."
echo "# Feel free to edit this file; your changes will persist when regenerating."
inspect_args
