#!/usr/bin/env bash
# This script was generated by bashly 1.2.10 (https://bashly.dev)
# Modifying it manually is not recommended

# :wrapper.bash3_bouncer
if [[ "${BASH_VERSINFO:-0}" -lt 4 ]]; then
  printf "bash version 4 or higher is required\n" >&2
  exit 1
fi

# :command.master_script

# :command.version_command
version_command() {
  echo "$version"
}

# :command.usage
claudio_usage() {
  if [[ -n $long_usage ]]; then
    printf "claudio\n\n"
    printf "  CLI unificado para o container Claude (sandbox Nix + Claude Code / OpenCode).\n  Sem subcomando abre Claude no diretório atual ou em DIR.\n\n"
  else
    printf "claudio - CLI unificado para o container Claude (sandbox Nix + Claude Code / OpenCode).\n\n"
  fi

  printf "%s\n" "Usage:"
  printf "  claudio [OPTIONS] [COMMAND]\n"
  printf "  claudio [COMMAND] --help | -h\n"
  printf "  claudio --version | -v\n"
  echo
  # :command.usage_commands
  printf "%s\n" "Commands:"
  printf "  %s   Abre Claude no container (sessão efêmera). Diretório atual ou DIR. (default)\n" "run     "
  printf "  %s   Abre OpenCode (TUI) com o projeto montado. DIR opcional.\n" "open    "
  printf "  %s   Abre bash no container com o projeto montado.\n" "shell   "
  printf "  %s   Retoma sessão anterior do Claude (--resume).\n" "resume  "
  printf "  %s   Continua conversa no Claude (--continue).\n" "continue"
  printf "  %s   Sobe sandbox persistente e abre Claude (equiv. make start).\n" "start   "
  printf "  %s   Sobe sandbox e abre OpenClaw gateway (LM Studio).\n" "openclaw"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    printf "%s\n" "Global Options:"

    # :command.usage_flags
    # :flag.usage
    printf "  %s\n" "--haiku"
    printf "    Usar modelo Claude Haiku\n"
    echo

    # :flag.usage
    printf "  %s\n" "--opus"
    printf "    Usar modelo Claude Opus\n"
    echo

    # :flag.usage
    printf "  %s\n" "--instance ID"
    printf "    Sufixo da instância (ex. 2 = clau-projeto-2)\n"
    echo

    # :flag.usage
    printf "  %s\n" "--rw"
    printf "    Montar projeto em read-write (default para run)\n"
    echo

    # :flag.usage
    printf "  %s\n" "--ro"
    printf "    Montar projeto read-only\n"
    echo

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo
    printf "  %s\n" "--version, -v"
    printf "    Show version number\n"
    echo

    # :command.usage_environment_variables
    printf "%s\n" "Environment Variables:"

    # :environment_variable.usage
    printf "  %s\n" "CLAUDIO_NIXOS_DIR"
    printf "    Raiz do repo NixOS (default $HOME/nixos)\n"
    echo

    # :environment_variable.usage
    printf "  %s\n" "OBSIDIAN_PATH"
    printf "    Caminho do vault Obsidian (default $HOME/.ovault)\n"
    echo

  fi
}

# :command.usage
claudio_run_usage() {
  printf "claudio run - Abre Claude no container (sessão efêmera). Diretório atual ou DIR.\n\n"
  printf "Alias: r\n"
  echo

  printf "%s\n" "Usage:"
  printf "  claudio [run] [DIR]\n"
  printf "  claudio run --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "%s\n" "Arguments:"

    # :argument.usage
    printf "  %s\n" "DIR"
    printf "    Diretório do projeto (default ~/projects)\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  claudio\n"
    printf "  claudio run ~/meu-projeto\n"
    printf "  claudio run --haiku\n"
    echo

  fi
}

# :command.usage
claudio_open_usage() {
  printf "claudio open - Abre OpenCode (TUI) com o projeto montado. DIR opcional.\n\n"
  printf "Alias: opencode, code\n"
  echo

  printf "%s\n" "Usage:"
  printf "  claudio open [DIR]\n"
  printf "  claudio open --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "%s\n" "Arguments:"

    # :argument.usage
    printf "  %s\n" "DIR"
    printf "    Diretório do projeto (default ~/projects)\n"
    echo

    # :command.usage_examples
    printf "%s\n" "Examples:"
    printf "  claudio open\n"
    printf "  claudio open . --rw\n"
    echo

  fi
}

# :command.usage
claudio_shell_usage() {
  printf "claudio shell - Abre bash no container com o projeto montado.\n\n"
  printf "Alias: sh\n"
  echo

  printf "%s\n" "Usage:"
  printf "  claudio shell [DIR]\n"
  printf "  claudio shell --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "%s\n" "Arguments:"

    # :argument.usage
    printf "  %s\n" "DIR"
    printf "    Diretório do projeto (default ~/projects)\n"
    echo

  fi
}

# :command.usage
claudio_resume_usage() {
  printf "claudio resume - Retoma sessão anterior do Claude (--resume).\n\n"

  printf "%s\n" "Usage:"
  printf "  claudio resume [DIR]\n"
  printf "  claudio resume --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "%s\n" "Arguments:"

    # :argument.usage
    printf "  %s\n" "DIR"
    printf "    Diretório do projeto (default ~/projects)\n"
    echo

  fi
}

# :command.usage
claudio_continue_usage() {
  printf "claudio continue - Continua conversa no Claude (--continue).\n\n"
  printf "Alias: cont\n"
  echo

  printf "%s\n" "Usage:"
  printf "  claudio continue [DIR]\n"
  printf "  claudio continue --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "%s\n" "Arguments:"

    # :argument.usage
    printf "  %s\n" "DIR"
    printf "    Diretório do projeto (default ~/projects)\n"
    echo

  fi
}

# :command.usage
claudio_start_usage() {
  printf "claudio start - Sobe sandbox persistente e abre Claude (equiv. make start).\n\n"

  printf "%s\n" "Usage:"
  printf "  claudio start [DIR]\n"
  printf "  claudio start --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

    # :command.usage_args
    printf "%s\n" "Arguments:"

    # :argument.usage
    printf "  %s\n" "DIR"
    printf "    Diretório do projeto (default ~/projects)\n"
    echo

  fi
}

# :command.usage
claudio_openclaw_usage() {
  printf "claudio openclaw - Sobe sandbox e abre OpenClaw gateway (LM Studio).\n\n"

  printf "%s\n" "Usage:"
  printf "  claudio openclaw\n"
  printf "  claudio openclaw --help | -h\n"
  echo

  # :command.long_usage
  if [[ -n "$long_usage" ]]; then
    printf "%s\n" "Options:"

    # :command.usage_fixed_flags
    printf "  %s\n" "--help, -h"
    printf "    Show this help\n"
    echo

  fi
}

# :command.normalize_input
# :command.normalize_input_function
normalize_input() {
  local arg passthru flags
  passthru=false

  while [[ $# -gt 0 ]]; do
    arg="$1"
    if [[ $passthru == true ]]; then
      input+=("$arg")
    elif [[ $arg =~ ^(--[a-zA-Z0-9_\-]+)=(.+)$ ]]; then
      input+=("${BASH_REMATCH[1]}")
      input+=("${BASH_REMATCH[2]}")
    elif [[ $arg =~ ^(-[a-zA-Z0-9])=(.+)$ ]]; then
      input+=("${BASH_REMATCH[1]}")
      input+=("${BASH_REMATCH[2]}")
    elif [[ $arg =~ ^-([a-zA-Z0-9][a-zA-Z0-9]+)$ ]]; then
      flags="${BASH_REMATCH[1]}"
      for ((i = 0; i < ${#flags}; i++)); do
        input+=("-${flags:i:1}")
      done
    elif [[ "$arg" == "--" ]]; then
      passthru=true
      input+=("$arg")
    else
      input+=("$arg")
    fi

    shift
  done
}

# :command.inspect_args
inspect_args() {
  if ((${#args[@]})); then
    readarray -t sorted_keys < <(printf '%s\n' "${!args[@]}" | sort)
    echo args:
    for k in "${sorted_keys[@]}"; do
      echo "- \${args[$k]} = ${args[$k]}"
    done
  else
    echo args: none
  fi

  if ((${#deps[@]})); then
    readarray -t sorted_keys < <(printf '%s\n' "${!deps[@]}" | sort)
    echo
    echo deps:
    for k in "${sorted_keys[@]}"; do
      echo "- \${deps[$k]} = ${deps[$k]}"
    done
  fi

  if ((${#env_var_names[@]})); then
    readarray -t sorted_names < <(printf '%s\n' "${env_var_names[@]}" | sort)
    echo
    echo "environment variables:"
    for k in "${sorted_names[@]}"; do
      echo "- \$$k = ${!k:-}"
    done
  fi
}

# :command.user_lib
# src/lib/compose_lib.sh
# Shared helpers for claudio CLI (compose file, mount path, project names).
# Sourced by generated script. Uses CLAUDIO_NIXOS_DIR, OBSIDIAN_PATH, args, flag_*.

claudio_nixos_dir="${CLAUDIO_NIXOS_DIR:-$HOME/nixos}"
claudio_compose_file="$claudio_nixos_dir/claudinho/docker-compose.claude.yml"
claudio_obsidian_path="${OBSIDIAN_PATH:-$HOME/.ovault}"

# Resolve mount directory: named arg "dir" (bashly uses args['dir']) or default ~/projects
claudio_resolve_dir() {
  local dir="${args[dir]:-$HOME/projects}"
  if [[ -n "$dir" ]]; then
    (cd "$dir" 2>/dev/null && pwd) || { echo "claudio: dir not found: $dir" >&2; exit 1; }
  else
    echo "$HOME/projects"
  fi
}

# Slug from dir basename (lowercase, alphanumeric + hyphen)
claudio_proj_slug() {
  local d="$1"
  basename "$d" | tr '[:upper:]' '[:lower:]' | tr -cs 'a-z0-9' '-' | sed 's/-*$//'
}

# Project name for claude (clau-SLUG or clau-SLUG-INSTANCE)
claudio_proj_name() {
  local slug="$1"
  local instance="${flag_instance:-}"
  local name="clau-${slug}"
  [[ -n "$instance" && "$instance" != "1" ]] && name="${name}-${instance}"
  echo "$name"
}

# Project name for opencode (persistent sandbox)
claudio_proj_name_open() {
  local slug="$1"
  echo "clau-${slug}-open"
}

# Mount opts: --rw (default for run) or --ro
claudio_mount_opts() {
  if [[ -n "${flag_rw:-}" ]]; then echo "rw"; elif [[ -n "${flag_ro:-}" ]]; then echo "ro"; else echo "rw"; fi
}

# Model flag for claude binary
claudio_model_flag() {
  if [[ -n "${flag_haiku:-}" ]]; then echo "--model claude-haiku-4-5-20251001"; elif [[ -n "${flag_opus:-}" ]]; then echo "--model claude-opus-4-6"; else echo ""; fi
}

# :command.command_functions
# :command.function
claudio_run_command() {

  # src/commands/run.sh
  mount_path="$(claudio_resolve_dir)"
  proj_slug="$(claudio_proj_slug "$mount_path")"
  proj_name="$(claudio_proj_name "$proj_slug")"
  mount_opts="$(claudio_mount_opts)"
  model="$(claudio_model_flag)"
  CLAUDIO_MOUNT="$mount_path" CLAUDIO_MOUNT_OPTS="$mount_opts" OBSIDIAN_PATH="$claudio_obsidian_path" \
    docker compose -f "$claudio_compose_file" -p "$proj_name" run --rm -it \
    --entrypoint /bin/bash -e CLAUDIO_MOUNT="$mount_path" sandbox \
    -c ". /workspace/host/scripts/bootstrap.sh; cd /workspace/mount && exec /home/claude/.nix-profile/bin/claude ${model} --permission-mode bypassPermissions"

}

# :command.function
claudio_open_command() {

  # src/commands/open.sh
  mount_path="$(claudio_resolve_dir)"
  proj_slug="$(claudio_proj_slug "$mount_path")"
  proj_name="$(claudio_proj_name_open "$proj_slug")"
  mount_opts="$(claudio_mount_opts)"
  echo "[claudio open] ${proj_slug} → ${proj_name} (mount: ${mount_opts})"
  CLAUDIO_MOUNT="$mount_path" CLAUDIO_MOUNT_OPTS="$mount_opts" \
    docker compose -f "$claudio_compose_file" -p "$proj_name" up -d sandbox
  CLAUDIO_MOUNT="$mount_path" CLAUDIO_MOUNT_OPTS="$mount_opts" \
    docker compose -f "$claudio_compose_file" -p "$proj_name" exec -it \
    -e CLAUDIO_MOUNT="$mount_path" sandbox bash -c 'cd /workspace/mount && exec opencode'

}

# :command.function
claudio_shell_command() {

  # src/commands/shell.sh
  mount_path="$(claudio_resolve_dir)"
  proj_slug="$(claudio_proj_slug "$mount_path")"
  proj_name="$(claudio_proj_name "$proj_slug")"
  mount_opts="$(claudio_mount_opts)"
  CLAUDIO_MOUNT="$mount_path" CLAUDIO_MOUNT_OPTS="$mount_opts" OBSIDIAN_PATH="$claudio_obsidian_path" \
    docker compose -f "$claudio_compose_file" -p "$proj_name" run --rm -it \
    --entrypoint /bin/bash -e CLAUDIO_MOUNT="$mount_path" sandbox

}

# :command.function
claudio_resume_command() {

  # src/commands/resume.sh
  mount_path="$(claudio_resolve_dir)"
  proj_slug="$(claudio_proj_slug "$mount_path")"
  proj_name="$(claudio_proj_name "$proj_slug")"
  mount_opts="$(claudio_mount_opts)"
  CLAUDIO_MOUNT="$mount_path" CLAUDIO_MOUNT_OPTS="$mount_opts" OBSIDIAN_PATH="$claudio_obsidian_path" \
    docker compose -f "$claudio_compose_file" -p "$proj_name" run --rm -it \
    --entrypoint /bin/bash -e CLAUDIO_MOUNT="$mount_path" sandbox \
    -c "cd /workspace/mount && exec /home/claude/.nix-profile/bin/claude --resume --permission-mode bypassPermissions"

}

# :command.function
claudio_continue_command() {

  # src/commands/continue.sh
  mount_path="$(claudio_resolve_dir)"
  proj_slug="$(claudio_proj_slug "$mount_path")"
  proj_name="$(claudio_proj_name "$proj_slug")"
  mount_opts="$(claudio_mount_opts)"
  model="$(claudio_model_flag)"
  CLAUDIO_MOUNT="$mount_path" CLAUDIO_MOUNT_OPTS="$mount_opts" OBSIDIAN_PATH="$claudio_obsidian_path" \
    docker compose -f "$claudio_compose_file" -p "$proj_name" run --rm -it \
    --entrypoint /bin/bash -e CLAUDIO_MOUNT="$mount_path" sandbox \
    -c ". /workspace/host/scripts/bootstrap.sh; cd /workspace/mount && exec /home/claude/.nix-profile/bin/claude --continue ${model} --permission-mode bypassPermissions"

}

# :command.function
claudio_start_command() {

  # src/commands/start.sh
  # Persistent sandbox + Claude (like legacy make start)
  mount_path="$(claudio_resolve_dir)"
  proj_slug="$(claudio_proj_slug "$mount_path")"
  proj_name="$(claudio_proj_name "$proj_slug")"
  mount_opts="$(claudio_mount_opts)"
  touch "$HOME/.claude.json"
  echo "[claudio start] ${proj_slug} → ${proj_name}"
  CLAUDIO_MOUNT="$mount_path" CLAUDIO_MOUNT_OPTS="$mount_opts" OBSIDIAN_PATH="$claudio_obsidian_path" \
    docker compose -f "$claudio_compose_file" -p "$proj_name" up -d sandbox
  CLAUDIO_MOUNT="$mount_path" CLAUDIO_MOUNT_OPTS="$mount_opts" OBSIDIAN_PATH="$claudio_obsidian_path" \
    docker compose -f "$claudio_compose_file" -p "$proj_name" exec -it \
    -e CLAUDIO_MOUNT="$mount_path" sandbox bash -c \
    '. /workspace/host/scripts/bootstrap.sh; cd /workspace; exec /home/claude/.nix-profile/bin/claude --permission-mode bypassPermissions'

}

# :command.function
claudio_openclaw_command() {

  # src/commands/openclaw.sh
  mkdir -p "$HOME/.openclaw"
  if [[ ! -f "$HOME/.openclaw/openclaw.json" ]]; then
    cp "$claudio_nixos_dir/.openclaw/openclaw.json" "$HOME/.openclaw/openclaw.json" 2>/dev/null || true
    echo "[openclaw] Config copiada para $HOME/.openclaw (LM Studio em host.docker.internal:1234)"
  fi
  docker compose -f "$claudio_compose_file" up -d sandbox
  docker compose -f "$claudio_compose_file" exec -it sandbox openclaw gateway

}

# :command.parse_requirements
parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --version | -v)
        version_command
        exit
        ;;

      --help | -h)
        long_usage=yes
        claudio_usage
        exit
        ;;

      # :flag.case
      --haiku)

        # :flag.case_no_arg
        args['--haiku']=1
        shift
        ;;

      # :flag.case
      --opus)

        # :flag.case_no_arg
        args['--opus']=1
        shift
        ;;

      # :flag.case
      --instance)

        # :flag.case_arg
        if [[ -n ${2+x} ]]; then
          args['--instance']="$2"
          shift
          shift
        else
          printf "%s\n" "--instance requires an argument: --instance ID" >&2
          exit 1
        fi
        ;;

      # :flag.case
      --rw)

        # :flag.case_no_arg
        args['--rw']=1
        shift
        ;;

      # :flag.case
      --ro)

        # :flag.case_no_arg
        args['--ro']=1
        shift
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.environment_variables_filter

  env_var_names+=("CLAUDIO_NIXOS_DIR")
  env_var_names+=("OBSIDIAN_PATH")

  # :command.command_filter
  action=${1:-}

  case $action in

    run | r)
      action="run"
      shift
      claudio_run_parse_requirements "$@"
      shift $#
      ;;

    open | opencode | code)
      action="open"
      shift
      claudio_open_parse_requirements "$@"
      shift $#
      ;;

    shell | sh)
      action="shell"
      shift
      claudio_shell_parse_requirements "$@"
      shift $#
      ;;

    resume)
      action="resume"
      shift
      claudio_resume_parse_requirements "$@"
      shift $#
      ;;

    continue | cont)
      action="continue"
      shift
      claudio_continue_parse_requirements "$@"
      shift $#
      ;;

    start)
      action="start"
      shift
      claudio_start_parse_requirements "$@"
      shift $#
      ;;

    openclaw)
      action="openclaw"
      shift
      claudio_openclaw_parse_requirements "$@"
      shift $#
      ;;

    # :command.command_fallback
    *)
      action="run"
      claudio_run_parse_requirements "$@"
      shift $#
      ;;

  esac

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.parse_requirements
claudio_run_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        claudio_run_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="run"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        # :argument.case
        if [[ -z ${args['dir']+x} ]]; then
          args['dir']=$1
          shift
        else
          printf "invalid argument: %s\n" "$key" >&2
          exit 1
        fi

        ;;

    esac
  done

}

# :command.parse_requirements
claudio_open_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        claudio_open_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="open"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        # :argument.case
        if [[ -z ${args['dir']+x} ]]; then
          args['dir']=$1
          shift
        else
          printf "invalid argument: %s\n" "$key" >&2
          exit 1
        fi

        ;;

    esac
  done

}

# :command.parse_requirements
claudio_shell_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        claudio_shell_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="shell"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        # :argument.case
        if [[ -z ${args['dir']+x} ]]; then
          args['dir']=$1
          shift
        else
          printf "invalid argument: %s\n" "$key" >&2
          exit 1
        fi

        ;;

    esac
  done

}

# :command.parse_requirements
claudio_resume_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        claudio_resume_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="resume"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        # :argument.case
        if [[ -z ${args['dir']+x} ]]; then
          args['dir']=$1
          shift
        else
          printf "invalid argument: %s\n" "$key" >&2
          exit 1
        fi

        ;;

    esac
  done

}

# :command.parse_requirements
claudio_continue_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        claudio_continue_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="continue"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        # :argument.case
        if [[ -z ${args['dir']+x} ]]; then
          args['dir']=$1
          shift
        else
          printf "invalid argument: %s\n" "$key" >&2
          exit 1
        fi

        ;;

    esac
  done

}

# :command.parse_requirements
claudio_start_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        claudio_start_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="start"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        # :argument.case
        if [[ -z ${args['dir']+x} ]]; then
          args['dir']=$1
          shift
        else
          printf "invalid argument: %s\n" "$key" >&2
          exit 1
        fi

        ;;

    esac
  done

}

# :command.parse_requirements
claudio_openclaw_parse_requirements() {
  # :command.fixed_flags_filter
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        claudio_openclaw_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  # :command.command_filter
  action="openclaw"

  # :command.parse_requirements_while
  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)
        # :command.parse_requirements_case
        # :command.parse_requirements_case_simple
        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

# :command.initialize
initialize() {
  declare -g version=""
  set -e

}

# :command.run
run() {
  # :command.globals
  declare -g long_usage=''
  declare -g -A args=()
  declare -g -A deps=()
  declare -g -a env_var_names=()
  declare -g -a input=()

  normalize_input "$@"
  parse_requirements "${input[@]}"

  case "$action" in
    "run") claudio_run_command ;;
    "open") claudio_open_command ;;
    "shell") claudio_shell_command ;;
    "resume") claudio_resume_command ;;
    "continue") claudio_continue_command ;;
    "start") claudio_start_command ;;
    "openclaw") claudio_openclaw_command ;;
  esac
}

if [[ "${BASH_SOURCE[0]}" == "${0}" ]]; then
  initialize
  run "$@"
fi
